interface:
  display_name: "Windows VM Control"
  short_description: "Control or delegate Windows work from Mac"
  default_prompt: "Use $windows-vm-control to choose a direct, bounded, or managed Windows workflow and verify the requested result."
